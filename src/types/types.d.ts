export type Villager = {
    name: string;
    id: string;
    personality: string;
    image_url: string;
}

export type VillagerDetails = {
    name: string;
    id: string;
    personality: string;
    image_url: string;
    species: string;
    gender: string;
  };